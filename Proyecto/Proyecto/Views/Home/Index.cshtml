@{
    ViewBag.Title = "Inicio";
}

<style>
    body {
        background-color: #1f1f1f;
        color: #e4e4e4;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    main.container {
        background-color: #2a2a2a;
        padding: 3rem 2rem;
        border-radius: 1rem;
        box-shadow: 0 0 30px rgba(0, 0, 0, 0.8);
    }

    .hero {
        background: #3b3b3b;
        border-radius: 1rem;
        padding: 3rem 2rem;
        margin-bottom: 3rem;
        box-shadow: 0 0 20px rgba(255, 165, 0, 0.1);
    }

        .hero h1,
        .hero p {
            color: #ffb14d; 
        }

    .card-custom {
        background-color: #444444;
        border: 1px solid #5a5a5a;
        border-radius: 1rem;
        transition: transform 0.3s ease, box-shadow 0.3s ease;
        color: #f1f1f1;
    }

        .card-custom:hover {
            transform: translateY(-6px);
            box-shadow: 0 12px 30px rgba(255, 165, 0, 0.2);
        }

    .card-icon {
        font-size: 3rem;
        color: #ff7f50; 
        margin-bottom: 1rem;
    }

    .btn-outline-primary,
    .btn-outline-danger {
        border-color: #ff7f50;
        color: #ff7f50;
    }

        .btn-outline-primary:hover,
        .btn-outline-danger:hover {
            background-color: #ff7f50;
            color: #121212;
        }

    a {
        text-decoration: none;
    }
</style>

<main class="container">
    <div class="hero text-center">
        <h1 class="display-4 fw-bold">Bienvenido a TareasFidelitas</h1>
        <p class="lead">Organiza, administra y cumple tus tareas de manera eficiente y profesional.</p>
    </div>

    <div class="row text-center">
        <div class="col-md-6 mb-4">
            <div class="card card-custom h-100 p-4">
                <div class="card-body">
                    <div class="card-icon"><i class="bi bi-check2-square"></i></div>
                    <h3 class="card-title">Gestión de tareas</h3>
                    <p class="card-text">Crea, edita y elimina tareas fácilmente para mantener tu flujo de trabajo ordenado.</p>
                    <a href="/tareas" class="btn btn-outline-primary mt-3">Ver tareas</a>
                </div>
            </div>
        </div>
        <div class="col-md-6 mb-4">
            <div class="card card-custom h-100 p-4">
                <div class="card-body">
                    <div class="card-icon"><i class="bi bi-exclamation-triangle"></i></div>
                    <h3 class="card-title">Tareas fallidas</h3>
                    <p class="card-text">Consulta y gestiona las tareas que no se han completado correctamente.</p>
                    <a href="/Dashboard" class="btn btn-outline-danger mt-3">Ir al panel</a>
                </div>
            </div>
        </div>
    </div>
</main>

<!-- Bootstrap Icons -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">